// Servicio de integración con OpenAI
// Encapsula la lógica para enviar y recibir mensajes de la API de OpenAI.

export async function sendToOpenAI(prompt: string): Promise<string> {
  // Aquí va la lógica para llamar a la API de OpenAI
  return 'Respuesta simulada de OpenAI';
}
